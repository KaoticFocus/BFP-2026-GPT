# Netlify Configuration for BuildFlow Pro AI
# ==========================================

[build]
  # Build from root - pnpm is pre-installed on Netlify
  # Use --prod=false to include devDependencies (typescript needed for build)
  # 1. Install ALL deps including devDeps
  # 2. Build shared package
  # 3. Build ui package
  # 4. Generate Prisma client, then build db package
  # 5. Build the web app
  command = "pnpm install --prod=false && pnpm --filter @buildflow/shared build && pnpm --filter @buildflow/ui build && cd packages/db && pnpm db:generate && pnpm build && cd ../.. && cd apps/web && pnpm build"
  
  # Publish the Next.js output
  publish = "apps/web/.next"

[build.environment]
  # Node version
  NODE_VERSION = "20"

# Next.js plugin for optimal deployment
[[plugins]]
  package = "@netlify/plugin-nextjs"

# Environment variable context
[context.production.environment]
  NODE_ENV = "production"

[context.deploy-preview.environment]
  NODE_ENV = "production"

# Headers for security
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
